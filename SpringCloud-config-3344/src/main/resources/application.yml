server:
  port: 3344
  
spring:
  application:
    name: springcloud-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/G631233828/SpringCloudConfig.git    #git仓库
          
      